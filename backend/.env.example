# Copia este archivo a `.env` y ajusta los valores.
# `.env` está en .gitignore (no se sube).
SESSION_SECRET=genera-un-valor-largo-y-aleatorio
# DATABASE_URL solo si el Postgres no corre en el contenedor `db`
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ordenamiento
